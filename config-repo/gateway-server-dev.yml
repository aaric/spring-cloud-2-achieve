# Zuul settings
zuul:
  routes:
    api-user-server:
      path: /api/usergroup/**
      serviceId: user-api-server
    api-order-server:
      path: /api/ordergroup/**
      serviceId: order-api-server
